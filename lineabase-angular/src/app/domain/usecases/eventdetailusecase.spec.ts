import { Eventdetailusecase } from './eventdetailusecase';

describe('Eventdetailusecase', () => {
  it('should create an instance', () => {
    expect(new Eventdetailusecase()).toBeTruthy();
  });
});
